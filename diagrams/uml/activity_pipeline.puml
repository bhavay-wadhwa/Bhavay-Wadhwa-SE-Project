@startuml
start
:Start camera or upload video;
:Capture frame;
:Preprocess frame (resize, normalize);
:Run YOLOv9 inference (src/detection.py);
if (detections found?) then (yes)
  :Apply NMS & filter low scores;
  :Update tracker (DeepSORT) (src/tracking.py);
  :Estimate distance for relevant objects (src/distance_estimation.py);
  :Check alerts & thresholds (src/alerts.py);
  if (alert triggered?) then (yes)
    :Trigger alert & log event;
  endif
  :Visualize boxes & distances (src/visualizer.py);
  :Stream/save annotated frame & logs;
else (no)
  :Optionally log no-detection;
endif
stop
@enduml

